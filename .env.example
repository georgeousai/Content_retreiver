TELEGRAM_BOT_TOKEN=
GROQ_API_KEY=
DATABASE_URL=postgresql://user:password@host:5432/dbname
# Optional. Without it reels are still downloaded and transcribed; only the
# reading of on-screen text is off.
GEMINI_API_KEY=
